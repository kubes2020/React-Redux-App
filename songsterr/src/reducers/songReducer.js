const initialState = {
  data: [],
  isLoading: false,
  error: "",
};

export const songReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
